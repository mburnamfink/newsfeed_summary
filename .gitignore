# Secrets & credentials
credentials.json
token.json
_token.json
anthropic_key.txt
.env

# Personal configuration & history (use preferences.example.yaml as a template)
preferences.yaml
feedback.yaml

# Library source of truth: per-article scores, tags, stars, feedback, read state,
# and the FTS body index. Personal; rebuildable from digests + archives.
articles.db
articles.db-*

# Generated output (digests, downloaded archives, read state)
serve/
digests/

# Background-run logs
logs/

# Local agent / editor settings
.claude/settings.local.json

# Python
*.pyc
__pycache__/
venv/
.venv/
*.egg-info/
build/
dist/
